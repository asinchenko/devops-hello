version: '3.8'

services:
  devops_hello:
    image: '${DOCKERHUB_USERNAME}/devops_hello:${CI_PIPELINE_ID}'
    ports:
      - '${DEPLOY_PORT}:8080'
    deploy:
      restart_policy:
        condition: on-failure
