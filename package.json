{
	"name": "devops-course-app",
	"version": "1.0.0",
	"description": "A simple web app for DevOps course",
	"main": "app.js",
	"scripts": {
		"start": "node app.js",
		"build": "npm install && npm prune --production && mkdir -p dist && cp -r . dist && rm -rf dist/node_modules"
	},
	"dependencies": {
		"express": "^4.17.1"
	}
}
