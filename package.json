{
	"name": "devops-course-app",
	"version": "1.0.0",
	"description": "A simple web app for DevOps course",
	"main": "app.js",
	"scripts": {
		"start": "node app.js",
		"build": "rm -rf dist && mkdir dist && cd dist && npm install --omit=dev"
	},
	"dependencies": {
		"express": "^4.17.1"
	}
}
