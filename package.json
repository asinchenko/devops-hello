{
	"name": "devops-course-app",
	"version": "1.0.0",
	"description": "A simple web app for DevOps course",
	"main": "app.js",
	"scripts": {
		"start": "node app.js",
		"build": "rm -rf dist && mkdir dist && cp -r . dist && cd dist && rm -rf node_modules && npm install --omit=dev"
	},
	"dependencies": {
		"express": "^4.17.1"
	}
}
